// ⚠️ TEMPORARILY DISABLED
// Direct PCA Map is postponed to avoid UI and methodological overload.
// The file is kept as a placeholder for future development.

/*
import { t, Lang } from '../i18n';

type Props = {
  lang: Lang;
};

export const DirectPCAMap = ({ lang }: Props) => {
  const tr = t(lang);

  return (
	<div className="direct-pca-page">
	  <h2>{tr.directPCATitle}</h2>

	  <div className="direct-pca-map-slot">
		// SVG / canvas will be added later
	  </div>
	</div>
  );
};
*/

export const DirectPCAMap = () => {
  return null;
};